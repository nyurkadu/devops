# Terraform Project with 3 Workspaces

This project contains Terraform workspace folders for:
- dev
- qa
- prod

Each workspace contains a text file with the workspace name and the workgroup name `avi_ws_gr`.
